import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  typography: {
    h4: {
      fontWeight: 100,
    },
    h6: {
      fontWeight: 100,
    },
    h2: {
      fontWeight: 100,
    },
  },
});

export default theme;
